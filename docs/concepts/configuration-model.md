# Configuration Model

Highest priority wins: CLI flags, Python API options, Markdown frontmatter, project config, user config, built-in defaults. This lets projects define defaults while single commands override them.
