# Input and Output Model

Markprint supports file input, raw Markdown strings, stdin, batch rendering, and multi-file compilation. Use `--base-url` for raw strings or stdin that reference relative assets.
