# Render Pipeline

The pipeline turns `MarkdownSource` into `PdfArtifact`. Each stage is intentionally independent so engines, themes, and post-processors can be swapped without changing the whole package.
